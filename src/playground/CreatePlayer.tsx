import React from 'react';

type CreatePlayerProps = {

};

const CreatePlayer: React.FC<CreatePlayerProps> = () => {

    return (
        <div className="my-component">
            <h2>Ein neuer Held ...</h2>

        </div>
    );
};

export default CreatePlayer;
